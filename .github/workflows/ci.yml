name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  lint-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run repository checks
        run: node scripts/ci-check.js
