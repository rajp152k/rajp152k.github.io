name: Update meditation index

on:
  push:
    branches: [master]
    paths:
      - "meditations/**/*.md"
      - "scripts/publish.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803

      - name: Generate meditation table
        run: python3 scripts/publish.py --readme

      - name: Commit updated index
        run: |
          if git diff --quiet -- README.md; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add README.md
          git commit -m "update meditation index [skip ci]"
          git push
